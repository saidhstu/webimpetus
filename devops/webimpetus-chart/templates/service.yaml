apiVersion: v1
kind: Service
metadata:
  name: {{ include "webimpetus-chart.fullname" . }}
  labels:
    {{- include "webimpetus-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "webimpetus-chart.selectorLabels" . | nindent 4 }}
