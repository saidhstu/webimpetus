on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ come_back_to_this_later ]
  workflow_dispatch:

name: Build and Deploy WebImpetus test using Docker compose in test env at https://test-my.workstation.co.uk/
# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: self-hosted
    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
    # - name: Run bash script to prepare dev environment a clean start
    #   run: |
    #       chmod 777 ./reset_env.sh
    #       chmod +x ./reset_env.sh
    #       sudo ./reset_env.sh
    #   shell: bash

    - name: Check out this repo code
      uses: actions/checkout@v3
  
    # - name: Run bash script to deploy to test server
    #   run: |
    #       sudo chmod +x ./build_and_deploy_test_env.sh
    #       sudo ./build_and_deploy_test_env.sh
    #   shell: bash

    - uses: isbang/compose-action@v1.1.0
      with:
        compose-file: "docker-compose.yml"
        up-flags: "--build"
        # services: |
        #   webserver
        #   database


    # - name: Slack Notification
    #   uses: rtCamp/action-slack-notify@v2
    #   env:
    #     SLACK_CHANNEL: general
    #     SLACK_COLOR: ${{ job.status }} # or a specific color like 'good' or '#ff00ff'
    #     SLACK_ICON: https://github.com/rtCamp.png?size=48
    #     SLACK_MESSAGE: 'Post Content :rocket:'
    #     SLACK_TITLE: Post Title
    #     SLACK_USERNAME: rtCamp
    #     SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}


